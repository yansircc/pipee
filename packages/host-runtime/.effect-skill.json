{
  "shape": ["library", "node-tool"],
  "executableEdges": [
    {
      "path": "test/lease-contender-worker.ts",
      "owner": "@runtime-tests",
      "reason": "process fixture owns one contender runtime and keeps an acquired SQLite lease scoped until process exit",
      "execution": {
        "lifecycle": "process-main",
        "runner": "NodeRuntime.runMain"
      }
    }
  ],
  "testGlobs": ["test/**/*.test.ts"],
  "hostTooling": [
    {
      "path": "vite.config.ts",
      "owner": "@vite-plus-tooling",
      "reason": "Vite+ task, lint, format, test, and scanner configuration is host tooling."
    }
  ]
}
