---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="信任边界 — Pipee" description="理解 Pi、Pipee 和 Extension 的数据与权限边界。"><div class="page"><section class="content-hero pad"><span class="eyebrow">Trust boundary</span><div><h1>谁拥有事实，谁负责解释。</h1><p>Pipee 不用“安全”替代具体边界。这里说明 Pi、Web UI 和 Extension 分别拥有、读取和呈现什么。</p></div></section><section class="owners pad content-section"><div class="content-section-head"><span class="eyebrow">Fact ownership</span><div><h2>一个事实，只有一个 owner。</h2></div></div><div class="owner-table surface"><div class="owner-row head"><span>Owner</span><span>拥有的事实</span><span>Pipee 中的表现</span></div><div class="owner-row"><strong>Pi</strong><p>Session、消息、模型调用、上下文和 active tools。</p><p>Pipee 读取并呈现，不维护副本作为新真相。</p></div><div class="owner-row"><strong>Pipee</strong><p>Web UI 的布局、视图偏好与本地界面状态。</p><p>只负责交互和投影，不接管 Agent runtime。</p></div><div class="owner-row"><strong>Extension</strong><p>自己的工具、服务、连接和领域状态。</p><p>必须独立说明能力、访问范围和停止方式。</p></div></div></section><section class="before pad content-section"><div class="content-section-head"><span class="eyebrow">Before install</span><div><h2>安装前应当能回答四个问题。</h2></div></div><ol><li><span>01</span><div><strong>增加什么能力？</strong><p>工具、事件、服务、Skill 和 Web Surface 分别列明。</p></div></li><li><span>02</span><div><strong>会访问什么？</strong><p>文件、浏览器、消息服务或外部 API 不藏在模糊描述里。</p></div></li><li><span>03</span><div><strong>代码来自谁？</strong><p>package、版本、维护者、源码与发布归档可以追溯。</p></div></li><li><span>04</span><div><strong>如何停止？</strong><p>长期资源随生命周期释放，用户知道如何禁用和卸载。</p></div></li></ol></section><section class="warning pad content-section"><div class="warning-card"><span class="flag">当前边界</span><div><h2>官方推荐不等于进程隔离。</h2><p>Package identity 是来源标识，不是安全沙箱。Web Surface 可以在受限 iframe 中运行，但真正不可信的运行时代码仍需要独立进程隔离。</p><p>因此，目录只展示来源和权限边界清楚的 Extension，不用一枚徽标替代安全模型。</p></div></div><a class="quiet-link" href="/extensions">查看 Extensions</a></section></div></BaseLayout>
<style>.owner-table{overflow:hidden}.owner-row{align-items:start;border-bottom:1px solid var(--line);display:grid;gap:28px;grid-template-columns:.55fr 1fr 1fr;padding:23px 26px}.owner-row:last-child{border-bottom:0}.owner-row.head{background:#eeeeeb;color:var(--dim);font:700 8px var(--mono);letter-spacing:.08em;text-transform:uppercase}.owner-row strong{font-size:18px}.owner-row p{color:var(--muted);font-size:12px;line-height:1.7;margin:0}.before{background:#eeeeeb}.before ol{background:#fff;border:1px solid var(--line);border-radius:10px;display:grid;grid-template-columns:1fr 1fr;list-style:none;margin:0;overflow:hidden;padding:0}.before li{display:grid;gap:25px;grid-template-columns:28px 1fr;padding:27px}.before li:nth-child(odd){border-right:1px solid var(--line)}.before li:nth-child(-n+2){border-bottom:1px solid var(--line)}.before li>span{font:700 8px var(--mono)}.before strong{font-size:18px}.before p{color:var(--muted);font-size:11px;line-height:1.7;margin:10px 0 0}.warning-card{border-left:3px solid var(--blue);display:grid;gap:40px;grid-template-columns:.45fr 1.55fr;padding:7px 0 7px 28px}.flag{color:var(--blue);font:700 9px var(--mono);letter-spacing:.08em;text-transform:uppercase}.warning h2{font-size:clamp(34px,4vw,52px);letter-spacing:-.055em;margin:0 0 25px}.warning p{color:var(--muted);font-size:13px;line-height:1.85;max-width:700px}.warning>.quiet-link{display:inline-block;margin:35px 0 0 28px}@media(max-width:700px){.owner-row{grid-template-columns:1fr}.owner-row.head{display:none}.before ol{grid-template-columns:1fr}.before li{border-bottom:1px solid var(--line)!important;border-right:0!important}.warning-card{grid-template-columns:1fr}}</style>
